# Tests run against the in-repo index — hermetic, no network, no ~/.m2 artifact needed.
# Surefire's working directory is the mcp module, so the relative path resolves to the index module.
knowledge.index.path=../index/src/main/resources/knowledge-index
