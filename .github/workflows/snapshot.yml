name: Deploy Snapshot

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: snapshot-deploy
  cancel-in-progress: true

jobs:
  deploy:
    if: github.repository == 'luigidemasi/camel-kit-knowledge' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: temurin
          cache: maven
          server-id: central
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD

      - name: Deploy snapshot
        run: ./mvnw -B -Psourcecheck -Dformat.skip=true -DdeployAtEnd=true deploy
        env:
          MAVEN_USERNAME: ${{ secrets.MVN_CENTRAL_USER }}
          MAVEN_PASSWORD: ${{ secrets.MVN_CENTRAL_PASSWORD }}
